import { Component } from './Component.js';

/**
 * @classDesc Node with HUD class is rendered in foreground (separate pipeline)
 */
export class HUD extends Component {
    constructor() {
        super();
    }
}
